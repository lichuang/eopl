#lang racket
#|
Exercise 3.15 [*] Extend the language by adding a new operation print that takes one argument, 
  prints it, and returns the integer 1. Why is this operation not expressible 
  in our speciﬁcation framework?
|#

; see extended-let-lang.rkt `unary-exp` op `print` case